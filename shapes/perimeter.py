def circle(r): 
    p = round((2 * 3.141592653589 * r), 2)
    return float(p)

def rectangle(w,l):
    p = round((2 * w + 2 * l), 2)
    return float(p)

def square(n):
    p = round((n * 4), 2)
    return float(p)

def triangle(a,b,c):
    p = round((a + b + c), 2)
    return float(p)