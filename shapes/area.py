def circle(r):
    a = round((3.141592653589 * (r ** 2)), 2)
    return float(a)

def rectangle(w,l):
    a = round((w * l), 2)
    return float(a)

def square(n):
    a = round((n ** 2), 2)
    return float(a)

def triangle(b,h):
    a = round((0.5 * b * h), 2)
    return float(a)